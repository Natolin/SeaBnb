<div class="container">
  <div class="row">
    <div class="col-xs-12">

      <div class="login-form-body">

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">

            <div class="login-profile-header">
              <h2>Log in</h2>
            </div>

            <%= f.input :email, required: false, autofocus: true %>
            <%= f.input :password, required: false %>

            <!-- This feature is not important for us! -->
            <!-- <div><%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %></div> -->
          </div>

          <div class="login-form-actions">
            <%= f.button :submit, "Log in", class: 'login-profile' %>
          </div>

        <% end %>
      </div>
    </div>
  </div>
</div>


<!-- This was no longer needed -->
<!-- <div><%= render "devise/shared/links" %></div> -->
